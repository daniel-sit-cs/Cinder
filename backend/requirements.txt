# FastAPI
fastapi==0.115.0
uvicorn[standard]==0.31.0
pydantic==2.9.0
python-multipart==0.0.12
python-dotenv

# AI — Replicate (image generation via API, no local GPU needed)
replicate

# Video compilation
moviepy
Pillow
numpy>=1.26,<2

# Text-to-speech
gTTS

# Firebase
firebase-admin

# HTTP
requests
